# =============================================================================
# DEPRECATED: This file is maintained for backward compatibility only.
# =============================================================================
# 
# This standalone compose file has been consolidated into the main
# docker-compose.yml at the project root using Docker Compose profiles.
#
# NEW USAGE (Recommended):
#   From project root:
#     docker compose -f ../docker-compose.yml --profile mcp-server-prod up --build -d
#
# OLD USAGE (Still works, but not recommended):
#   From this directory:
#     docker compose -f compose.yml up --build -d
#
# =============================================================================
# For production deployment, use the main compose file with profile:
#   cd ../../
#   docker compose --profile mcp-server-prod up --build -d
#
# The main compose file includes:
#   - Production-grade security hardening
#   - Resource limits
#   - Health checks
#   - Proper networking
# =============================================================================

name: wazuh-main-server

services:
  wazuh-main-server:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BUILD_DATE: ${BUILD_DATE}
        PYTHON_VERSION: ${PYTHON_VERSION:-3.13}
        VERSION: ${VERSION:-4.1.0}
      target: production

    image: wazuh-main-server:${VERSION:-4.1.0}
    container_name: wazuh-main-server
    hostname: wazuh-main-server
    
    restart: unless-stopped
    init: true
    
    env_file: 
      - .env
    environment:
      # Runtime overrides
      MCP_HOST: 0.0.0.0
      MCP_PORT: 3000
      ENVIRONMENT: production
      # Authentication mode: oauth (recommended), bearer (default), none (dev only)
      AUTH_MODE: ${AUTH_MODE:-bearer}
      
    ports:
      - "${MCP_PORT:-3000}:3000"
    
    # Modern health check with curl
    healthcheck:
      test: ["CMD-SHELL", "curl -sf --max-time 5 http://localhost:3000/health | jq -e '.status == \"healthy\"' > /dev/null"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 45s
      
    # Resource management
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 128M
    
    # Security hardening
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    
    # Writable directories
    tmpfs:
      - /tmp:noexec,nosuid,size=64m
      - /app/logs:noexec,nosuid,size=128m
    
    # Structured logging
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
        labels: "service=wazuh-main-server"
    
    # Service labels
    labels:
      - "com.docker.compose.service=wazuh-main-server"
      - "org.opencontainers.image.title=Wazuh MCP Remote Server"
      - "org.opencontainers.image.description=MCP-compliant remote server for Wazuh SIEM (main branch)"
      - "monitoring.prometheus.scrape=true"
      - "monitoring.prometheus.port=3000"
      - "monitoring.prometheus.path=/metrics"
      - "branch=main"
